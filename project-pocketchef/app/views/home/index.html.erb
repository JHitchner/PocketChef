
  <%= form_tag({:controller => 'home', :action => 'index'}, method: 'get') do %>
    <label for="search"></label>
    <%= text_field_tag :search, nil, :placeholder => "What Do You Wanna Make?" %>
      <%= submit_tag "Let's Get Cooking", :name => nil, :id => "submitHome" %>
  <% end %>


<div class="searchImages">
  <% @recipeResponse["hits"].each do |response| %>
    <span id="imageResponse">
      <p id= "homeTitle"><%= response["recipe"]["label"] %></p>
      <% @image = response["recipe"]["image"] %>
    <%= link_to image_tag(@image), {:controller => "recipes", :action => "index", :search => response["recipe"]["label"]} %>
    </span>
<%end%>
</div>
